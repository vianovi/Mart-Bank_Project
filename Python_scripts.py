# Impor modul yang diperlukan
import json
import os
import datetime
import time
import hashlib # Untuk hashing password
import uuid    # Untuk ID unik
import re      # Untuk validasi input (misalnya email, format PIN)